#!/bin/sh
message=$(head -n 1 "$1")
case "$message" in
  feat:*|fix:*|docs:*|style:*|refactor:*|perf:*|test:*|build:*|ci:*|chore:*|revert:* ) exit 0 ;;
  * ) echo "Commit message must follow Conventional Commits (for example: feat: add renewal policy)" >&2; exit 1 ;;
esac
